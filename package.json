{
  "name": "mcppure-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "mcp-servers/*"
  ],
  "scripts": {
    "deploy": "bun run build:web && bun run deploy:hub",
    "build:web": "cd apps/web && bun run build && rm -rf ../hub/public/* && cp -r out/* ../hub/public/",
    "dev:hub": "cd apps/hub && bun dev",
    "dev:web": "cd apps/web && bun dev",
    "deploy:hub": "cd apps/hub && bun run deploy",
    "deploy:web": "cd apps/web && bun run pages:build",
    "install:all": "bun install"
  }
}
